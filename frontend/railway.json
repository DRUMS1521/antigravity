{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "serve -s dist --listen tcp://0.0.0.0:${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE"
  }
}
